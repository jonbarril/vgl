// Neutralized backup stored outside of `src` to avoid compile-time issues.
package backups.status_command;

/**
 * Neutral placeholder kept in `backups/status-command` for merging.
 */
public final class StatusCommandBackupPlaceholder {
    // intentionally empty
}
